// Utility to extract stanza IDs from markdown
// Usage: const ids = parseStanzaShortcodes(markdown)
export function parseStanzaShortcodes(markdown) {
  // ...parsing logic will go here...
  return [];
}
